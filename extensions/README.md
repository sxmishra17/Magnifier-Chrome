# Extensions

Place each browser extension in its own folder under this directory.

Suggested naming:

- `chrome-<name>` for Chrome-first projects
- `firefox-<name>` for Firefox-specific projects
- `crossbrowser-<name>` for shared WebExtension projects

Current projects:

- [magnifier-chrome](./magnifier-chrome)